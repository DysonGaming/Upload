using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HoverAnim : MonoBehaviour
{

    void animate() 
    {
        // a switch that selects the chosen animation button and mode
        
        // case StartButtonON: Will activate the trigger to large
        // case StartButtonOFF: Will activate the trigger to small
    }


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
